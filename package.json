{
	"name": "kulagin-brand-backend",
	"version": "1.0.0",
	"scripts": {
		"db:enter": "docker exec -it directus_postgres psql -U directus -d postgres",
		"db:backup": "docker exec -t directus_postgres pg_dump -U directus directus > ./backend/backups/backup.sql",
		"db:restore": "docker exec -i directus_postgres psql -U directus -d directus < ./backend/backups/backup.sql",
		"schema:backup": "docker exec -it directus_app npx directus schema snapshot ./backend/backups/schema.yaml",
		"schema:restore": "docker exec -it directus_app npx directus schema apply ./backend/backups/schema.yaml"
	},
	"packageManager": "pnpm@10.17.1"
}
